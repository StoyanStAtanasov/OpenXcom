# OpenXcom Browser TypeScript Port - Agent Memory

This repo is being translated from OpenXcom C++ into a browser TypeScript port under `web/`. Treat this file as project-local operating memory for future agents.

## Mission

Translate the game faithfully into TypeScript for the browser.

The C++ source in `src/` is the authority. The TypeScript target in `web/src/` should preserve as much as practical:

- class names, method names, file grouping, enum names, constants
- UI ids, `setInterface()` categories, `add()` element ids
- coordinates, dimensions, list columns, row order, resource names
- control flow, algorithms, state-stack behavior, and save/model semantics

Do not invent a simpler game. Translate the game that is already written and debugged in C++.

## Reusable Skill

A local skill exists at:

`C:\Users\stoia\.codex\skills\openxcom-ts-port`

Use it for this project. Keep it updated when repeated lessons appear.

Every failure is a learning moment. After a failed build, verifier, merge, or assumption, ask what assumption was wrong and encode the improved rule in this file or the skill.

## Work Style

- Inspect the current worktree before trusting memory.
- Use `rg` / `rg --files` first.
- Read the C++ `.cpp` and `.h` before editing TypeScript.
- Read nearby translated TypeScript patterns before introducing a new style.
- Use `apply_patch` for manual edits.
- Do not revert unrelated user or agent changes.
- Keep generated `web/dist` as build output from `web/src`; regenerate with `npm run build`.
- Keep `web/PORTING.md` current after completed slices.

## Bulk Translation

Use subagents aggressively when the user asks for scale. Split by disjoint write scopes:

- One worker for one UI family, such as crafts, research, manufacture, diary, psi training.
- One worker for model/rules support, such as `StatString`, `ResearchProject`, `Production`, diary stats.
- One worker for verification or source analysis when integration can proceed locally.

Current operating mode is translate-first. Prefer direct first-pass ports of many source files over repeatedly polishing one slice. It is acceptable to retranslate a file later when an integration failure or better pattern proves the first pass was weak.

Worker prompt requirements:

- Keep prompts small, especially for Spark/small models.
- Name exact C++ source files and exact TypeScript target files.
- Tell workers they are not alone in the codebase and must not revert others.
- Require direct translation: preserve C++ ids, coordinates, algorithms, field names, constructor flow, and handler names.
- Require final changed-file list, build result, and explicit pending source boundaries.

Reliable spawning lessons:

- Treat worker IDs as exact opaque values. Copy them from `spawn_agent` output or `agent_path`; do not retype from memory.
- Keep an active assignment ledger mentally or in notes. Before spawning, check active workers' owned files; do not assign a file just because it looks small.
- Close completed workers promptly to free slots, then spawn replacement work with a new disjoint write scope.
- Avoid giving workers hot integration files unless their slice owns that integration. Keep `BasescapeState.ts`, `GeoscapeState.ts`, shared model files, and `web/PORTING.md` under the main integrator unless explicitly assigned.
- Worker final answers must be summary-first: changed files, build/verifier result, pending boundaries. The main agent should not need to read the whole implementation before deciding the next integration target.
- A failed close, merge conflict, or unexpected worker edit is an assumption audit: check whether IDs, ownership, or prompt boundaries were too loose and tighten the next prompt.

Model-use experiments:

- Use Spark only for small, isolated translation shells or source scans with exact write scopes and forbidden hot files. Keep prompts tiny; if Spark hits context/usage limits, reclaim the batch locally or respawn with a shorter file-owned prompt instead of spending time on process.
- Do not use Spark for shared model integration, cross-slice merge resolution, verifier diagnosis, or files already owned by active workers.
- Use higher-effort frontier reasoning for ambiguous source semantics, multi-file integration, and failure analysis. Simple wiring, docs updates, and mechanical verifier edits usually do not need extra-high effort.

Experiment log discipline:

- Record experiments as hypothesis -> result -> rule.
- Keep entries short and actionable; do not paste long transcripts.
- Promote repeated successful results into the main workflow; promote failures into guardrails.
- Track model/tool experiments separately from game translation progress so percentage reports stay about the port, not the process.
- Canonical learning entry schema: assumption -> observed result -> corrected rule -> location changed -> impact.
- Where to log: put experiment/journey details in `AGENTS.md`; put only durable generalized rules in the `openxcom-ts-port` skill to avoid drift.

Resource discipline:

- Prefer one focused verifier over broad repeated checks when credits are tight.
- Do not spawn new sidecars just to be busy; spawn only when their output will change the next integration decision.
- Reuse existing dashboard/build/typecheck signals instead of rerunning all gates after docs-only edits.
- Save high-effort reasoning for integration failures, source ambiguity, and cross-file model work.

Percentage definitions:

- File path parity: generated by `npm run status`; same-path source units with matching `web/src` TypeScript files divided by total source units.
- Slice percent: human estimate of source behavior translated for the named planned source files, after subtracting explicit pending boundaries.
- Area percent: human estimate for a gameplay/system area, informed by path parity plus integrated/verified slice status.
- Whole-port percent: conservative human estimate of playable browser parity, not just file count.

Integration is single-threaded. Review worker changes, resolve overlaps, run build, then run focused browser verification.

Context management rule: keep workers focused on translation details and the main agent focused on integration. Do not load every worker's full implementation into main context immediately. First read the worker's final summary and changed-file list, then inspect only the files for the slice currently being integrated.

## Context Packets

Use generated context packets to avoid rereading long narrative files after compaction or before spawning workers:

```powershell
cd web
npm run context
npm run context -- --slice "Geoscape popup batches"
npm run context:readonly
npm run context:worker
npm run orchestrator
npm run codex:status
npm run agents
npm run agents:prompt -- --role readonly --task "audit time30Minutes support" --scope "src/Geoscape/GeoscapeState.cpp; web/src/Geoscape/GeoscapeState.ts"
```

This refreshes `web/translation-status.json`, then writes `web/CONTEXT_PACKET.md` and `web/context-packet.json`.

- Read `web/CONTEXT_PACKET.md` first after a resume or compaction.
- Use `--slice` when a sidecar or worker should focus on one named slice instead of the default active slice.
- Use `npm run context:readonly` for audit/source-scan sidecars and `npm run context:worker` for bounded code workers; both generate prompt skeletons and reduce hand-written prompt context.
- Use `npm run orchestrator` for a compact main-agent dashboard: path parity, active slice, active agent ledger, and locally observable Codex context/rate-limit data.
- Use `npm run codex:status` from `web/` or `node tools/codex-status.mjs` from repo root for the local session JSONL status reader. Credits may still be unavailable when Codex does not write them locally.
- Use `npm run agents:start` before spawning an agent to record its exact id/scope, then `npm run agents:close` when it returns. The ledger refuses overlapping active scopes unless `--allow-overlap` is explicitly passed after review.
- Use `npm run agents:prompt` to generate compact handoff prompts from the current context packet instead of rewriting the full project rules by hand.
- For no-fork read-only sidecars, pass only the packet plus the exact file list to inspect.
- For worker agents, pass the packet's active slice fields, exact write scope, forbidden hot files, boundaries, and final-format requirements.
- Regenerate the packet after changing `web/translation-slices.json`, finishing a verified micro-path, or changing the active integration queue.
- Treat `web/TRANSLATION_STATUS.md` as generated dashboard output and `AGENTS.md` as policy/learning memory, not as the live numeric source.
- `web/agent-ledger.json` is ignored local coordination state; the durable rule is the script/workflow, not the session-specific agent list.

## Translation Pattern

For translate-first sweeps:

1. Pick unowned missing C++ pairs by directory or class family.
2. Add direct TS ports with original class/method/field names.
3. Leave explicit source-named boundaries for unavailable browser/runtime systems.
4. Run light build/typecheck checkpoints after broad batches, not after every tiny file.
5. Retranslate weak first-pass files when a better local pattern is learned.

For integration/debug slices:

1. Read source C++ and headers.
2. Identify existing translated dependencies.
3. Add only helpers required by the source path.
4. Preserve source-shaped boundaries for unported child states.
5. Wire call sites only after the translated state exists.
6. Build.
7. Verify behavior in browser when UI/runtime changed.
8. Clean temporary verifier artifacts.
9. Update `web/PORTING.md`.
10. Report translation percentages.

## Browser Verification

Use Playwright CLI verifiers for UI/runtime slices.

Temporary verifier file shape:

```js
async page => {
  await page.goto("http://127.0.0.1:4173/web/index.html");
  await page.waitForFunction(() => window.openxcomGame?.getMod());
  const result = await page.evaluate(async () => {
    // import from /web/dist, build source-shaped save/model state,
    // push translated states, drive handlers, assert model/text/list behavior.
    return { ok: true };
  });
  await page.evaluate(value => console.log(`VERIFY_SLICE ok ${JSON.stringify(value)}`), result);
}
```

Important learned verifier rules:

- Wait for the specific rules and language strings needed, not just `getMod()`.
- Console logs from Node-side `console.log` may not enter the browser log; emit `VERIFY_*` via `page.evaluate`.
- For large Playwright verifier bodies on Windows, store the script under `output/playwright`, load it from the page with `page.evaluate(fetch(...).text())`, and execute it through a short `run-code` wrapper.
- In `playwright-cli run-code`, fetch temporary verifier files inside the browser page context, not the Node-side wrapper; the wrapper environment may not expose `fetch`.
- File-backed verifier text must be an expression when wrapped as `eval('(' + text + ')')`; omit the trailing semicolon after the top-level `async page => { ... }`.
- When checking queued geoscape popups, call the translated state `think()` after the timer method because C++/TS queue popups first and push them to the game stack in the next think cycle.
- Directly driving state handlers is often faster and more reliable than pixel clicking.
- Check model state, list text, row ordering, visibility flags, and source-specific side effects.
- If the translated method re-resolves rules by id through `Mod.getItem()` / `getResearch()`, register those fake rules in the verifier's fake mod lookup. Passing a rule object directly to one source path does not prove another path can look it up by id.
- When a narrow verifier starts calling a broader translated C++ method, expand fake objects with default source methods such as `getSpecialType()` or `getSavedBattle()` instead of weakening the translated method to match the old fixture.

Cleanup after browser checks:

- close Playwright CLI browser
- delete the temporary `output/playwright/verify-*.js`
- remove `.playwright-cli`
- remove temporary `open-out.log` / `open-err.log`

Do not delete existing screenshot evidence in `output/playwright` unless it was created as a temporary artifact for the current check.

## Current Lessons

- The user values faithful source translation over new design.
- A passing build is not enough; Node's type stripping can hide type-only missing imports.
- When adding typed fields, inspect imports manually.
- Temporary reflective helpers such as `call()` / `callArray()` are allowed only as explicit boundaries; once the owning model slice lands, replace them with typed source-shaped methods.
- For an ephemeral TypeScript check, use `npx --yes --package typescript tsc --noEmit` or `tsc --version`; `npx --yes typescript ...` fails because the package name is not the executable name.
- When a screen uses `BACK*.SCR`, add manifest and `Mod.loadOptionalScrSurface()` support if missing.
- Memorial/dead-soldier screens reverse the dead-soldier list and pass null base into `SoldierInfoState`.
- Soldier armor and sack behavior must return old armor to stores exactly as the C++ does.
- PowerShell expands `$skill-name` inside double-quoted strings; quote or escape skill metadata carefully.
- This Windows PowerShell environment may reject `&&` command chaining; run dependent commands as separate tool calls or use a repo script instead of relying on shell separators.
- Failed verifier waits usually mean the wait condition was too weak. Wait for the exact loaded rules/language needed by the screen.
- Browser load is staged; waiting for one rule family is not enough when the scenario needs later data such as `startingBase.rul`. Wait for the complete model scenario that the verifier will use.
- Verifiers must respect source click guards. Do not assume row 0 is actionable when C++ filters by state such as `craft.getStatus() != "STR_OUT"`; select a model row that satisfies the source condition.
- Direct-handler verifiers must use the translated source handler names exactly, including original casing such as `btnOKClick` versus `btnOkClick`.
- Every fix should trigger an assumption audit, then a durable rule update.
- Protect context: delegate detailed source reading to workers, then integrate one returned slice at a time from changed files and summaries.
- Reliable agent spawning depends on exact copied IDs, disjoint write scopes, explicit forbidden files, and summary-first worker output.
- For progress/doc audits, use no-fork read-only sidecars with a tight file list; the main agent should keep integration and verification decisions local.
- Treat model choice as an experiment with feedback: record which tasks Spark handles safely and where it should be avoided; do not spend extra-high reasoning on low-ambiguity mechanical work.
- Treat 100% source path parity as an inventory milestone, not project completion. The real finish line is verified behavior through source-shaped runtime slices and explicit remaining boundaries.
- When a route is already translated but a slice percentage is stuck behind a vague integration boundary, write a direct state-stack verifier for the exact C++ entry/exit branches before moving the percentage. For terminal routes, drive `init()`, `think()`, and button handlers directly with source-shaped saves instead of relying only on indirect UI coverage.
- Compound UI primitives should not be reduced to convenient browser widgets. If the C++ class owns child surfaces and modal routing, such as `ComboBox` owning a `TextButton`, popup `Window`, and `TextList`, translate that structure and verify child event timing directly.
- Console-log route markers are not translations when the C++ child state exists. Replace them with the source `pushState(new ChildState(...))` route and verify the stack effect.
- Startup loader text and cursor visibility are browser adapter seams. Do not blindly preserve DOS/4GW, SoundBlaster IRQ/DMA, or hidden-native-cursor assumptions when the canvas CSS hides the browser pointer.
- Font rendering bugs need a full source display-path audit: C++ scale defaults, `Screen` base surface size, browser CSS scale, and indexed PNG shade recovery all affect whether the original bitmap font appears correctly.
- Assumption: startup music verification only needed native streams or a MIDI backend. Observed result: once `MUSIC_ADLIB` was translated, the source priority correctly selected ADLIB.CAT for GMSTORY when no streamable digital file was present, and the old verifier rejected real translated playback. Corrected rule: music verifiers must accept all source-priority playback routes that are translated for the installed data set: stream, MIDI backend, or Adlib/YM3812 custom playback. Location changed: `AdlibMusic.ts`, `fmopl.ts`, `Mod.ts`, `Music.ts`, `verify-adlib-music-player.mjs`, `verify-startup-browser.mjs`. Impact: no more treating a faithful source fallback as a missing-music bug.
- Source bitmap font PNGs must be treated as indexed images, not grayscale art. C++ `Surface::loadImage()` disables lodepng color conversion and copies raw palette indices, so `FontSmall.png` maps `255 -> 1`, `207 -> 2`, `159 -> 3`, `111 -> 4`, `63 -> 5`; use `npm run verify:font-source-parity` before accepting font shade fixes.
- Browser audio startup is also an adapter seam: never create or resume `AudioContext` before browser user activation. Install one-shot pointer/key/touch unlock listeners and let pre-gesture sounds be silent instead of logging autoplay-policy warnings.
- Spark usage-limit status can contradict stale local `/status` data. If a Spark sidecar returns a usage-limit error, close it and continue locally or use another model; do not retry the same unavailable bucket.
- YAML order matters in hand-written TS parsers only if we accidentally make it matter. Source YAML loading accepts mappings in any order, so parsers such as `parseStartingBaseRul()` must handle TFTD craft rows where `type` is not the first key.
- When C++ methods pass contextual neighbor objects such as `tileBelow`, preserve that parameter through the full call chain. Do not collapse it to a simpler setter because status/floating side effects may live in the callee.
- On Windows Node verifier helpers, do not spawn `npm.cmd` directly with `shell: false`; route `.cmd` through `cmd /d /s /c` or reuse an existing verifier `run()` helper.
- Pathfinding verifiers should assert source behavior, not incidental path shape. A* can route around one blocked tile with diagonal moves in the same number of steps, so check that the blocked straight-line path changed instead of assuming a longer path.
- User-facing marker scans need reachability/source-owner reconciliation before calling something a real missing branch. A stale dead helper is not a gameplay gap; a menu path that still logs or throws at the C++ call site is.
- When retiring a source route marker, wire the existing translated child state directly and tighten the parent to the shared source model contract. If that exposes transitive local placeholder types, fix the callers and shared type instead of weakening the route back to `any`.

## Experiment Log

- Hypothesis: Spark can handle small isolated translation shells. Result: first Spark spawn was shut down because its files overlapped an active worker before edits landed. Rule: check active assignment ownership before every spawn, including small files.
- Hypothesis: `npx --yes typescript --version` can check the TypeScript version. Result: npm could not determine the executable. Rule: use `npx --yes --package typescript tsc ...`.
- Hypothesis: waiting for loaded craft rules is enough for basescape craft verifier setup. Result: `newSave()` could still lack starting craft data. Rule: wait for the complete scenario data the verifier will use.
- Hypothesis: an ephemeral `tsc --noEmit` can provide useful extra signal even though the project builds with Node type stripping. Result: TypeScript 6.0.3 found two local `StatisticsState` type issues and two broader `SavedGame.getDifficultyCoefficient()` battlescape boundaries. Rule: fix localized new slice errors; do not derail integration into unrelated model gaps.
- Assumption: the learning docs were clear enough after adding an experiment log. Observed result: self-improvement sidecar found duplication risk and undefined percentage metrics. Corrected rule: `AGENTS.md` is canonical for experiments; the skill stores durable rules; percentages use defined meanings. Location changed: `AGENTS.md` experiment discipline and percentage definitions. Impact: future progress reports should be comparable instead of vibes-based.
- Assumption: path-parity coverage alone would be a useful progress metric. Observed result: progress watcher found it internally consistent but easy to overread, especially `Basescape` at 100% path parity while behavior boundaries remain. Corrected rule: show status rollups, next integration queue, and slice path warnings beside path parity. Location changed: `web/scripts/translation-status.mjs`, `web/TRANSLATION_STATUS.md`, `web/translation-status.json`. Impact: dashboard now separates file inventory from integration/verification state.
- Assumption: more parallel checks are always better. Observed result: user warned credits are low. Corrected rule: when credits are tight, finish the current integration with the narrowest proof, avoid speculative sidecars, and defer broad checks unless code changed in that risk area. Location changed: `AGENTS.md` resource discipline. Impact: keep throughput while respecting reset/credit pressure.
- Assumption: a small manual JSON slice edit was too simple to need immediate structural inspection. Observed result: the first patch left a duplicate `status` key before it was caught by targeted review. Corrected rule: after hand-editing dashboard JSON, inspect the edited slice before generating status because duplicate JSON keys can parse silently. Location changed: `AGENTS.md` experiment log. Impact: prevents misleading progress data.
- Assumption: waiting for soldier rules was enough before a stat-string verifier called `newSave()`. Observed result: mod loading is staged and `newSave()` could run before starting-base soldiers were populated. Corrected rule: wait for the full scenario signal needed by the verifier, or test the direct source hook (`Mod.genSoldier()`) when that is the behavior under integration. Location changed: `AGENTS.md` experiment log. Impact: avoids false verifier failures from partially loaded browser mod state.
- Assumption: a psi-training verifier could assert a hard row count and literal `YES`/`NO` text. Observed result: starting soldiers can already exist and loaded language strings may differ in case/text. Corrected rule: map model rows back to the specific object under test and compare UI labels through `Language.getString()`. Location changed: `AGENTS.md` experiment log. Impact: focused UI verifiers stay source-shaped without being brittle to scenario population or localization.
- Assumption: adding `SoldierDiary` ownership to `Soldier` could use ordinary runtime imports. Observed result: the browser failed before game init with `Cannot access 'State' before initialization` because savegame startup pulled battlescape/UI runtime through diary dependencies. Corrected rule: when attaching model helpers to startup-owned types, keep heavy downstream modules type-only or mirror tiny numeric enums locally, then verify page startup. Location changed: `SoldierDiary.ts`, `Soldier.ts`, `SavedGame.ts`, and `AGENTS.md`. Impact: soldier diaries are owned by soldiers without creating import cycles.
- Assumption: `SavedGame.getDifficultyCoefficient()` could be closed with a local difficulty-number fallback. Observed result: C++ uses the mutable `Mod::DIFFICULTY_COEFFICIENT` table, which rulesets can override through `vars.rul`. Corrected rule: translate shared C++ statics as shared TS statics when multiple source owners need them, instead of hardcoding a caller-local fallback. Location changed: `ModStatics.ts`, `Mod.ts`, `SavedGame.ts`, and `AGENTS.md`. Impact: strict `tsc --noEmit` is clean and AI/dogfight/monthly code read the source-shaped coefficient table.
- Assumption: `/status` rate-limit data was unavailable to the agent because it was only visible in the TUI. Observed result: Codex writes machine-readable `token_count` events with `rate_limits` into `~/.codex/sessions/**/*.jsonl`, and the installed/open-source CLI can be inspected before assuming a feature is inaccessible. Corrected rule: check Codex source/install files and local session JSONL for tool-state data, then build small local readers when useful. Location changed: `tools/codex-status.mjs` and `AGENTS.md`. Impact: main-agent budget decisions can use `node tools/codex-status.mjs --session <id>` without waiting for pasted `/status`.
- Assumption: `playwright-cli run-code` could accept a whole verifier body or import a local verifier module. Observed result: Windows hit command-line length limits, CLI `run-code` lacked a dynamic import callback, and CLI-side `fetch` was unavailable. Corrected rule: keep large verifiers in temporary `output/playwright/verify-*.js` files, load the file text from the browser page, execute it with a short wrapper, and emit `VERIFY_*` from `page.evaluate`. Location changed: `AGENTS.md` and the temporary statistics verifier. Impact: future UI checks can stay compact in chat context while remaining reproducible.
- Assumption: doc/progress auditing needed the main agent's full context. Observed result: a no-fork Spark sidecar with a strict read-only file list produced a concise statistics-slice doc audit while the main agent continued verification. Corrected rule: use no-fork sidecars for bounded audits that save context, then let the main agent reconcile stale or conservative recommendations against local build/verifier evidence. Location changed: `AGENTS.md`. Impact: parallel work can improve throughput without flooding the main context.
- Assumption: a worker-built geoscape popup batch should wait until the entire batch can be integrated before status changes. Observed result: the source-backed `GeoscapeState::btnInterceptClick` path could be integrated and browser-verified independently, while timer/monthly/target popups remain broader boundaries. Corrected rule: for large worker batches, promote verified micro-paths to `partial-integrated-verified`, keep the full slice active, and document the next integration boundary explicitly. Location changed: `GeoscapeState.ts`, geoscape resource manifest loading, `translation-slices.json`, and `PORTING.md`. Impact: progress becomes measurable without pretending the whole batch is done.
- Assumption: a save/model class can stay a narrow shell until its direct methods are needed. Observed result: `Globe.getTargets()` exposed that `AlienBase` inherits `Target` in C++ and needs longitude, latitude, id, and name/default-name behavior before it can be used honestly as a globe target. Corrected rule: when a translated C++ class inherits a shared source base like `Target`, translate the inherited contract at the first real consumer instead of casting around missing methods. Location changed: `AlienBase.ts`, `SavedGame.ts`, `Globe.ts`, and `AGENTS.md`. Impact: geoscape target picking can use typed target methods and strict `tsc --noEmit` remains clean.
- Assumption: future agents could save context by manually skimming `AGENTS.md`, `PORTING.md`, and generated status docs. Observed result: the docs contain useful but duplicated narrative, while workers only need a compact active-slice packet with source files, target files, boundaries, forbidden hot files, and verification commands. Corrected rule: use `npm run context` to generate `web/CONTEXT_PACKET.md` and `web/context-packet.json`, then pass only the relevant packet fields to sidecars/workers. Location changed: `web/scripts/context-pack.mjs`, `web/package.json`, `AGENTS.md`, `web/PORTING.md`, and the `openxcom-ts-port` skill. Impact: resumed turns and subagents start from a small generated handoff instead of filling context with long status prose.
- Assumption: a local popup-only `Waypoint` class was enough until full geoscape runtime integration. Observed result: destination confirmation, saved waypoints, craft return/patrol, target lists, and later timers all need the same source `Target` / `MovingTarget` / `Waypoint` contract. Corrected rule: when a screen-local class is actually a C++ savegame/model class, promote it to the matching `web/src/Savegame` file before wiring more callers. Location changed: `Target.ts`, `MovingTarget.ts`, `Waypoint.ts`, `Craft.ts`, and geoscape destination popups. Impact: popup code now calls source-shaped craft/target methods instead of optional runtime shims.
- Assumption: `playwright-cli run-code` could be wrapped in PowerShell single quotes with an unparenthesized fetched verifier body. Observed result: the CLI produced JavaScript parse errors before running the verifier. Corrected rule: on PowerShell, pass a double-quoted `async (page) => { ... }` wrapper and evaluate fetched verifier text as `eval('(' + text + ')')`. Location changed: `AGENTS.md`; temporary verifier was cleaned after use. Impact: future file-backed browser verifiers avoid command-line length limits and PowerShell parse traps.
- Assumption: a read-only sidecar summary can be taken as source fact after a tight prompt. Observed result: the sidecar correctly identified the useful low-fuel/waypoint micro-path but misstated part of the C++ `time5Seconds()` guard, which local source inspection caught. Corrected rule: use sidecars to shortlist branches and verifier ideas, but reconcile any detailed control-flow claim against the exact C++ before editing integration files. Location changed: `AGENTS.md`. Impact: parallelism still saves context, while the main integrator remains responsible for source fidelity.
- Assumption: local agent-ledger mutations could be run in parallel like read-only shell inspections. Observed result: two simultaneous `npm run agents:start` calls raced on the same JSON ledger and each command only reported its own active record; the spawned read-only agents still completed, but ledger state was unreliable. Corrected rule: run ledger start/close mutations sequentially; only parallelize read-only inspections or independent build/status commands. Location changed: `AGENTS.md` and `openxcom-ts-port` skill. Impact: sidecar coordination stays trustworthy while still using agents for bounded source audits.
- Hypothesis: Spark can safely handle tight read-only source audits for context saving. Result: two Spark explorers produced useful summary-first audits for graph persistence and Geoscape wiring, but one verifier assumption about January-equivalent month labels required main-thread source reconciliation. Rule: Spark is useful for bounded read-only checklists; the integrator must still verify detailed source semantics before patching.
- Assumption: all craft-to-UFO arrival behavior had to wait for full dogfight scheduling. Observed result: several C++ `time5Seconds()` UFO branches are independent and verifiable now: lost UFO last-known waypoint, destroyed/crashed no-payload return, and payload landing confirmation. Corrected rule: split timer integration by source branch when a branch has a stable translated popup/model surface, and leave only the actually coupled branch as the boundary. Location changed: `GeoscapeState.ts`, `translation-slices.json`, `PORTING.md`, and `AGENTS.md`. Impact: Geoscape progress moves without forcing premature dogfight scheduler integration.
- Assumption: a geoscape popup verifier could assert the top game state immediately after calling `time5Seconds()`. Observed result: `GeoscapeState.popup()` queues the state and `GeoscapeState.think()` pushes it on the stack, matching the C++ deferred popup flow. Corrected rule: verifier setup for timer-driven geoscape popups must call the timer method, then the state's `think()`, then assert the game stack. Location changed: `AGENTS.md`; temporary alien-base verifier was corrected and cleaned. Impact: avoids false negatives while preserving the original queued-popup lifecycle.
- Assumption: the existing context packet was enough for all subagent roles. Observed result: a read-only tooling sidecar found the packet still exposed long verification prose, stale session-specific status commands, and hand-written prompt work. Corrected rule: generate role-specific context packets and prompt skeletons with short `VERIFY_*` markers, no hardcoded Codex session id, and documented `--slice` use. Location changed: `web/scripts/context-pack.mjs`, `web/package.json`, `AGENTS.md`, and `web/PORTING.md`. Impact: main-thread context is smaller and subagent prompts are more repeatable.
- Assumption: a file-backed Playwright verifier could keep the natural trailing semicolon after `async page => { ... }`. Observed result: `eval('(' + text + ')')` parsed `(async page => { ... };)` and failed before running the verifier. Corrected rule: temporary verifier files loaded as expressions must omit the top-level trailing semicolon. Location changed: `AGENTS.md` and `openxcom-ts-port` skill. Impact: future file-backed verifiers avoid a preventable parse failure.
- Assumption: a clean `npm run build` after timer integration was enough to proceed to browser verification. Observed result: build passed, but `tsc --noEmit` caught a missing `CraftErrorState` import that would have broken the `time1Hour` rearm-error branch. Corrected rule: after adding any geoscape popup branch, run strict typecheck before the browser verifier even if the build passes. Location changed: `GeoscapeState.ts` and `AGENTS.md`. Impact: keeps source-branch imports honest despite Node type stripping.
- Assumption: `timerReset()` should unpause the translated geoscape timer because many popup buttons use it before returning to geoscape. Observed result: C++ `timerReset()` only presses the 5-seconds speed button; `_gameTimer` still ticks once after popups and `timeAdvance()` clears `_pause`. Corrected rule: keep `timerReset()` speed-only and let geoscape `think()` run `_gameTimer.think()` even when `_pause` is true but no popup/dogfight is active. Location changed: `GeoscapeState.ts` and `AGENTS.md`. Impact: queued popup flows can resume without inventing pause semantics.
- Assumption: the current context packet alone was enough to coordinate parallel agents. Observed result: completed read-only sidecars were useful, but active ownership still depended on main-thread memory and copied chat IDs. Corrected rule: use `web/scripts/agent-ledger.mjs` to generate prompts and record active agent ids/scopes before spawning, then close them promptly. Location changed: `web/scripts/agent-ledger.mjs`, `web/package.json`, `web/scripts/context-pack.mjs`, and `AGENTS.md`. Impact: future bulk translation can use more agents with lower collision/context risk.
- Assumption: a Geoscape verifier could use `mod.newSave().getBases()[0]` once core rule lists were loaded. Observed result: the first browser run hit an empty/incomplete fixture before the populated starting-base scenario was ready. Corrected rule: wait for the exact `newSave()` scenario shape and select the base with the needed facilities/craft instead of assuming base index 0. Location changed: temporary `VERIFY_GEOSCAPE_TIME30` verifier and `AGENTS.md`. Impact: future timer verifiers should fail on translated behavior, not staged fixture setup.
- Assumption: `STR_LASER_WEAPONS` would directly unlock `STR_LASER_PISTOL` manufacture in a daily-research verifier. Observed result: the original xcom1 manufacture rule for `STR_LASER_PISTOL` requires `STR_LASER_PISTOL`, while `STR_LASER_WEAPONS` only unlocks that separate research topic. Corrected rule: for research/manufacture verifiers, inspect the parsed `manufacture.rul` requirements and pick a topic whose completed research name exactly satisfies the intended manufacture rule, such as `STR_MOTION_SCANNER`. Location changed: temporary `VERIFY_GEOSCAPE_TIME1DAY_RESEARCH` verifier and `AGENTS.md`. Impact: verifier fixtures now follow original rules instead of assumed tech-tree semantics.
- Assumption: a generic context packet plus the repo-root Codex status command was enough for subagent/orchestrator work. Observed result: the generated handoff had a `node tools/codex-status.mjs` command that was wrong from `web/`, and a Spark sidecar found several low-risk reliability gaps. Corrected rule: expose runnable `web` scripts (`npm run codex:status`, `npm run orchestrator`), fail fast on unknown `--slice`, normalize Windows ledger scopes for overlap checks, and escape generated Markdown tables. Location changed: `web/package.json`, `web/scripts/context-pack.mjs`, `web/scripts/orchestrator-status.mjs`, `web/scripts/agent-ledger.mjs`, and `web/scripts/translation-status.mjs`. Impact: context-saving tooling is less dependent on memory and copy/paste precision.
- Assumption: the base-defense slice needed to wait for all dogfight/confirmation work before measurable integration progress. Observed result: the C++ UFO-to-base branch in `GeoscapeState::time5Seconds()` was independently source-backed and verifiable with direct browser model setup. Corrected rule: split large worker-built geoscape slices by source branch when the branch has stable model/state surfaces, then keep the parent slice `partial-integrated-verified` with the remaining dogfight/ConfirmLanding boundaries explicit. Location changed: `GeoscapeState.ts`, `Base.ts`, `translation-slices.json`, `PORTING.md`, and `AGENTS.md`. Impact: slice progress moves from 70% to 80% without overstating dogfight or BriefingState parity.
- Assumption: once `BattlescapeGenerator.run()` typechecked, the confirmation-to-briefing path would behave like C++. Observed result: the real Small Scout browser verifier first failed on unloaded embedded MCD dataset objects, then on missing `RuleCraft` deployment coordinates, even though build and typecheck were clean. Corrected rule: generator slices need a real mission verifier, `loadTerrainMapDataSets()` must rebind terrain datasets to the loaded `Mod` objects, and `RuleCraft.getDeployment()` must be translated before X-COM craft spawning can be considered faithful. Location changed: `BattlescapeGenerator.ts`, `RuleCraft.ts`, confirmation/briefing states, `translation-slices.json`, `PORTING.md`, and `AGENTS.md`. Impact: `VERIFY_CONFIRM_BRIEFING` now proves crash-recovery generation, X-COM/alien placement, briefing OK, and base-defense briefing.
- Assumption: continued micro-slice verification was the best use of effort after enough porting patterns were known. Observed result: the user pointed out that translation is cheap and weak files can be retranslated later, while a Spark worker also failed from too much prompt/context. Corrected rule: use translate-first sweeps with tiny file-owned worker prompts, defer deep QA until broad coverage lands, and retranslate files when better patterns appear. Location changed: `AGENTS.md` and `openxcom-ts-port` skill. Impact: main effort shifts from chasing one polished slice to raising source path parity quickly.
- Assumption: pasted `/status` Spark quota was enough to plan Spark workers. Observed result: Spark spawn still failed with a real usage-limit error, while `gpt-5.4-mini` workers completed tight file-owned translation batches and reported build results. Corrected rule: treat Spark availability as empirical per spawn; after one limit failure in a turn, fall back to mini/local translation instead of retrying Spark. Location changed: `AGENTS.md` and `openxcom-ts-port` skill. Impact: bulk translation keeps moving without wasting turns on stale quota signals.
- Hypothesis: broad path-parity finishing can be parallelized by source support directories. Result: Engine utility, audio/video, Ufopaedia, Scalers, Adlib, and root/header support units fit cleanly into disjoint owned target lists, with the main agent only integrating build/status. Rule: for translate-first endgame, split by exact directory/file ownership, forbid hot integration files, and keep workers' final output to changed files, build result, and explicit native/browser boundaries.
- Assumption: the local Codex status reader could report Spark quota once Spark reset. Observed result: the session JSONL `token_count` events still only exposed the active `gpt-5.5` primary/weekly buckets, but a `gpt-5.3-codex-spark` explorer spawned and completed a tight DogfightState audit. Corrected rule: use `npm run codex:status` for observable active-model budget, and treat a tiny no-fork Spark spawn as the empirical Spark availability check when the separate Spark bucket is not present locally.
- Assumption: Dogfight runtime fidelity should be attacked only after a full dogfight visual/audio pass. Observed result: `DogfightState::btnMinimizedIconClick()` and the destroyed-UFO retaliation branch were independently source-backed and verifiable through direct browser state setup. Corrected rule: split DogfightState by C++ branch when a branch touches stable translated models, and prove each branch with direct handler/private-method browser verifiers before broader visual/audio polish.
- Assumption: all `not translated yet` strings in the scan represented live source behavior. Observed result: `ActionMenuState` and `Pathfinding` markers were stale or invariant guards, while the real playable blocker was the normal save/load menu still missing `SavedGame::getList/getSaveInfo` and source file-operation wiring. Corrected rule: map every marker to its C++ call path and prioritize reachable menu/runtime seams; remove or downgrade false markers only after source inspection. Location changed: `SavedGame.ts`, save/load menu states, `verify-save-menu.mjs`, `PORTING.md`, and `translation-slices.json`. Impact: save/load progress is measured from source behavior instead of noisy text search.
- Assumption: a compact Node verifier could spawn `npm.cmd` directly for its build step. Observed result: Windows returned a null status before the verifier ran. Corrected rule: route npm from Node verifiers through `cmd.exe /d /s /c npm.cmd ...`, then keep the rest of the verifier small and deterministic. Location changed: `web/scripts/verify-save-menu.mjs`. Impact: the save-menu verifier now reproduces build plus storage assertions without Playwright overhead.
- Assumption: landing texture/shade parity was only a `GeoscapeState` call-site replacement. Observed result: C++ passes `Texture*`, not a `Surface`, and the useful data comes from `globe.rul` texture metadata plus `Globe::getPolygonTextureAndShade()`; a first verifier failed until the UFO fixture satisfied the source `getDetected()` guard. Corrected rule: for landing confirmation, translate the whole source handoff (`RuleGlobe` texture metadata -> `Globe` texture/shade lookup -> `ConfirmLandingState` -> `BattlescapeGenerator`) and make verifier fixtures satisfy earlier C++ guards before asserting the target branch.
- Assumption: repeating small shell probes was acceptable during an integration slice. Observed result: repeated `Get-Content`, `Get-ChildItem`, build/typecheck/browser/status calls wasted time and context, and the user explicitly called this out. Corrected rule: when a loop repeats twice, turn it into a repo script or delegate bounded inspection to a Spark/mini worker; keep the main agent on integration decisions and one final command. Location changed: `web/scripts/verify-geoscape-confirmations.mjs`, `web/package.json`, and `AGENTS.md`. Impact: future slice verification should be one compact command plus one summary, not a long sequence of manual probes.
- Assumption: a Spark worker's summary that it copied the full Dogfight blob table was enough after build passed. Observed result: a compact parity script found the TS table had 7 blobs instead of the C++ 8x13x13 table, and strict typecheck also caught a local `const` mutation. Corrected rule: use workers for bounded translation throughput, but verify copied numeric tables/resources with automated source-vs-target parity checks before accepting the worker result. Location changed: `DogfightState.ts`, `AGENTS.md`, and the geoscape confirmations verifier script. Impact: worker speed stays useful while source-data fidelity is machine-checked.
- Assumption: direct `execFile` of `npx.cmd` would behave like an interactive `npm run typecheck` on Windows. Observed result: Node 24 returned `spawn EINVAL` in the automation script while the npm script succeeded. Corrected rule: for recurring verification, prefer existing `npm run` scripts or `cmd /c npm run ...` from Node on Windows instead of re-encoding long `npx` invocations in multiple places. Location changed: verifier automation task assigned to Spark. Impact: cross-platform verification scripts should reuse the repo's known-good command wrappers.
- Assumption: `playwright-cli run-code --filename` could run either a code body or a function expression interchangeably. Observed result: the CLI expects a function argument; marker failures were opaque until the script included run-code stdout. Corrected rule: keep file-backed Playwright verifier files as `async page => { ... }`, include run-code stdout in script failures, and wait for the exact loaded rules used by the verifier such as `Mod.getInventory("STR_GROUND")`. Location changed: `web/scripts/verify-geoscape-confirmations.mjs` and `AGENTS.md`. Impact: one-command verifiers now fail with the real source dependency or fixture-method gap instead of silent marker misses.
- Assumption: a minimal fake battle unit was enough to verify `InventoryState` handoff. Observed result: `InventoryState.btnOkClick()` follows source pre-battle cleanup into `SavedBattleGame.resetUnitTiles()`, requiring unit armor/tile/position/setTile/visibility methods. Corrected rule: when a verifier drives the next state after a handoff, the fixture must satisfy the downstream source methods it invokes, or the verifier should assert only the handoff. Location changed: `web/scripts/verify-geoscape-confirmations.mjs` and `AGENTS.md`. Impact: route verifiers now prove the actual stack transition and first OK path without broadening into a full battle simulation.
- Assumption: the debriefing recovery verifier could reuse a fake corpse rule object without registering it in the fake `Mod`. Observed result: `DebriefingState::recoverAlien()` follows the C++ path and calls `Mod.getItem(corpseBattlescape[0])`, so the no-containment corpse fallback stat was absent until the fake rule was registered by id. Corrected rule: verifier fixtures must support both object-passed and id-relooked-up rule paths when the source mixes them. Location changed: `web/scripts/verify-debriefing.mjs` and `AGENTS.md`. Impact: future recovery/manufacture/research verifiers should catch translated lookup behavior instead of only testing direct object plumbing.
- Assumption: the existing debriefing helper verifier could keep its old minimal fake rules/save after `prepareDebriefing()` was expanded. Observed result: the broader source path immediately needed `RuleItem.getSpecialType()` and optional `SavedGame.getSavedBattle()` support before it could reach the behavior under test. Corrected rule: when a verifier graduates from isolated helper calls to a parent C++ method, upgrade the fake factory to the parent's source contract and add an actual parent-path scenario. Location changed: `web/scripts/verify-debriefing.mjs`, `DebriefingState.ts`, and `AGENTS.md`. Impact: verifier fixtures now move with translation scope instead of accidentally pinning the implementation to an obsolete narrow stub.
- Assumption: testing `DebriefingState::btnOkClick()` could use a tiny save fake because the method itself only checks month/ironman/autosave. Observed result: source-shaped child state constructors immediately read their own save/base dependencies, such as `PromotionsState` calling `getBases()`. Corrected rule: OK-stack verifiers must either provide downstream constructor dependencies or assert only branches whose child constructors can honestly initialize. Location changed: `web/scripts/verify-debriefing.mjs` and `AGENTS.md`. Impact: route-stack checks now prove actual translated child-state construction rather than just method call order.
- Assumption: sync XHR binary loaders could use `responseType = "arraybuffer"` like async fetch paths. Observed result: the browser rejected `responseType` for synchronous document XHR, so CAT sound loading silently stayed empty until the audio verifier checked `Mod.getSound("GEO.CAT", Mod.UFO_FIRE)`. Corrected rule: for source-shaped sync binary loaders, use `overrideMimeType("text/plain; charset=x-user-defined")` and convert `responseText` to bytes; keep async fetch for manifest-checked optional resources. Location changed: `CatFile.ts`, `Sound.ts`, `Music.ts`, and the geoscape confirmations verifier. Impact: original `SAMPLE.CAT` / `SOUND2.CAT` audio loads in the browser path.
- Assumption: probing optional music files with fetch was harmless because missing files returned null. Observed result: expected missing `GMGEO3..10` and `GMTACTIC1..10` MID probes produced browser console errors that correctly failed the verifier. Corrected rule: have `build.mjs` emit original-data file lists, then check the manifest before probing optional CAT/MID resources. Location changed: `web/scripts/build.mjs`, `Mod.ts`, and `verify-geoscape-confirmations.mjs`. Impact: verifiers stay strict about unexpected console errors without being noisy about known-absent optional files.
- Assumption: a dashboard with all tracked slices `integrated-verified` meant the next queue was empty. Observed result: `PORTING.md` and slice boundaries still named deeper runtime gaps, especially debriefing mission resolution. Corrected rule: when tracked slices are green but documented boundaries remain, create a new runtime slice for the boundary instead of treating the dashboard as completion proof. Location changed: `translation-slices.json`, `DebriefingState.ts`, `verify-debriefing.mjs`, and `AGENTS.md`. Impact: progress metrics continue to reflect real game parity work after path parity reaches 100%.
- Assumption: a verifier fake `isResearched()` returning false for every requirement would be enough for debriefing recovery. Observed result: C++ `SavedGame::isResearched(vector)` returns true for an empty requirements list, so ammo and ordinary recovered items should not be counted as alien artifacts unless their rule has actual unmet requirements. Corrected rule: verifier save fixtures must preserve empty-list semantics for research-gated recovery/manufacture paths. Location changed: `verify-debriefing.mjs` and `AGENTS.md`. Impact: recovery tests now distinguish source scoring from fixture artifacts.
- Hypothesis: a worker can cheaply draft failing acceptance coverage while the main agent translates the hot runtime files. Result: a Spark worker added diary/commendation/promotion verifier assertions that failed on the missing debriefing block, then the main agent implemented the C++ path and tightened only the new fake methods (`getMurdererId`, soldier stat getters) plus a real `SavedGame.handlePromotions` check. Rule: delegate disjoint verifier expansion to workers, keep integration local, and convert the worker's first failure into the acceptance target instead of re-reading the whole slice. Location changed: `web/scripts/verify-debriefing.mjs`, `DebriefingState.ts`, `SavedGame.ts`, `Soldier.ts`, `Options.ts`, `translation-slices.json`, and `AGENTS.md`.
- Assumption: debriefing containment/storage follow-up parity could be claimed from stack-order checks alone. Observed result: the useful proof came from letting `ManageAlienContainmentState` and `SellState` constructors run with source-shaped base fixtures, then directly driving `ManageAlienContainmentState.btnOkClick()` for corpse conversion and `canSellLiveAliens` funds. Corrected rule: when a parent state delegates to an already translated child, verify both the parent push order and one child source action from that call-site before retiring the boundary. Location changed: `web/scripts/verify-debriefing.mjs`, `translation-slices.json`, `PORTING.md`, and `AGENTS.md`.
- Assumption: CraftEquipmentState's side-array HWP shim was harmless because the UI verifier passed. Observed result: the source model already had `Craft.getVehicles()`, while storage and space accounting needed real `Vehicle` objects, `Craft.getSpaceAvailable()`, craft HWP rule-size storage, and ignored rearming clips. Corrected rule: when a UI state has a private translated model shadow, replace it with the owning savegame model before expanding verifier coverage. Location changed: `CraftEquipmentState.ts`, `Craft.ts`, `Base.ts`, `verify-craft-management.mjs`, `translation-slices.json`, `PORTING.md`, and `AGENTS.md`.
- Hypothesis: a Spark worker can safely draft verifier coverage for a hot route while the main agent translates the runtime path. Result: Carver added a bounded `verify-craft-management.mjs` handoff check in parallel, and the first post-translation run exposed missing fake `RuleItem` C++ methods (`isRifle`, `isPistol`, `getInventoryWidth`, `getInventoryHeight`) needed by the real `InventoryState` constructor. Rule: delegate disjoint verifier expansion with strict write scope, then tighten the fake factories to the child state's source contract when the translated parent actually reaches that child. Location changed: `web/scripts/verify-craft-management.mjs`, `CraftEquipmentState.ts`, `BattlescapeGenerator.ts`, `Soldier.ts`, `translation-slices.json`, `PORTING.md`, and `AGENTS.md`.
- Assumption: the craft inventory verifier fixture was complete once `InventoryState` could construct. Observed result: driving `InventoryState.init()` reached `BattleUnit.getCarriedWeight()`, and the fake `RuleItem` still needed source `getWeight()` before template/clear/autoequip assertions could run. Corrected rule: route verifiers that enter `InventoryState` must cover constructor plus init/stat contracts in the fake rule factory: `getInventoryWidth()`, `getInventoryHeight()`, `isRifle()`, `isPistol()`, and `getWeight()`. Location changed: `web/scripts/verify-craft-management.mjs`, `AGENTS.md`, and `openxcom-ts-port` skill. Impact: future inventory verifiers should fail on translated behavior rather than fixture surface gaps.
- Assumption: a verifier worker could keep ownership of a hot verifier until it returned a final patch. Observed result: the Spark worker timed out after adding useful but over-broad `Inventory.mouseClick` coverage with a private-method fallback, so the main agent closed it, simplified the assertion to the public Ctrl quick-move branch, and integrated the failing production invariant locally. Corrected rule: use workers for disjoint draft coverage, but if they time out or leave private fallbacks, close them and reduce the verifier to one public source path before debugging production. Location changed: `web/scripts/verify-craft-management.mjs`, `Inventory.ts`, and `AGENTS.md`. Impact: worker speed helps find targets without letting half-finished harness logic drive the integration.
- Assumption: direct `Action` fixtures could click inventory coordinates using only `RuleInventory.getX()/getY()`. Observed result: `InventoryState.centerAllSurfaces()` / `lowerAllSurfaces()` shifted the actual `Inventory` surface, so synthesized clicks were off by the surface offset and resolved to no slot until the verifier added `invWidget.getX()/getY()` to the absolute mouse position. Corrected rule: direct UI-handler verifiers must build absolute screen coordinates from local rule coordinates plus the target surface offset, then sanity-check slot resolution when failures look like no-ops. Location changed: `web/scripts/verify-craft-management.mjs`, `AGENTS.md`, and `openxcom-ts-port` skill. Impact: future mouse-driven verifiers should fail on behavior, not on centered-surface coordinate drift.
- Assumption: adding more inventory equipment at fixture setup was harmless when broadening the craft inventory verifier. Observed result: adding the third rifle before the storage accounting checks changed `Base.getUsedStores()` and broke an unrelated earlier assertion. Corrected rule: when a verifier needs extra setup for a later scenario, mutate the fixture after earlier source-accounting assertions that depend on the original inventory counts. Location changed: `web/scripts/verify-craft-management.mjs` and `AGENTS.md`. Impact: broader verifiers can add later scenario data without invalidating earlier focused checks.
- Assumption: a right-click inventory verifier could reuse the same ground-item click coordinates after a state-changing click. Observed result: proximity grenade activation follows the source and calls `arrangeGround(false)`, so the second right-click must recompute visible ground coordinates from the item's current slot. Corrected rule: after a verified action redraws or rearranges ground inventory, recompute mouse coordinates from the item state before driving the next click. Location changed: `web/scripts/verify-craft-management.mjs` and `AGENTS.md`. Impact: repeated item-click verifiers now track source redraw side effects instead of clicking stale pixels.
- Assumption: two read-only agent ledger `agents:start` commands could be recorded in parallel because the workers were not writing code. Observed result: the ledger is a single JSON writer and the parallel starts briefly left only one active entry. Corrected rule: ledger mutation is always sequential; parallelize the agent work, not the local ownership-record writes. Location changed: `AGENTS.md`. Impact: active worker scope tracking stays reliable while using more agents.
- Hypothesis: Spark sidecars are useful for context-saving audits if they get exact file lists and summary-first output. Result: two read-only Spark explorers quickly separated the live-alien boundary into "runtime already translated" versus "save/load persistence missing" and proposed verifier scenarios without loading their full file reads into the main context. Rule: for progress/gap audits, spawn small read-only explorers with hard file scopes and consume only their concise findings; keep source integration and final edits in the main thread.
- Assumption: adding `SavedGame.load()` could import `MissionStatistics` as a runtime value because the type already appeared in the file. Observed result: the browser verifier failed before loading with `Cannot access 'State' before initialization`, caused by a circular battlescape import chain pulled in by the runtime import. Corrected rule: when a save/model file currently uses `import type`, do not convert it to a value import unless the runtime dependency is proven acyclic; keep persistence slices focused on plain data or add a smaller loader. Location changed: `SavedGame.ts`, `verify-debriefing.mjs`, and `AGENTS.md`. Impact: browser startup remains strict against hidden circular dependencies.
- Assumption: a MonthlyReportState route verifier could use a minimal fake soldier because the test only cared about pushed follow-up states. Observed result: the constructor computes base maintenance before the route is clicked, and `Base.getPersonnelMaintenance()` called `soldier.getRules()`. Corrected rule: when a state constructor computes unrelated source aggregates, either provide the full model methods or override the aggregate at the save/base boundary so the fixture fails only on the target branch. Location changed: `web/scripts/verify-geoscape-confirmations.mjs` and `AGENTS.md`. Impact: monthly-route tests now prove `CommendationState` / `PsiTrainingState` / `SaveGameState` / `CutsceneState` ordering instead of maintenance fixture shape.
- Assumption: JSON metadata edits are safe to do with a quick script. Observed result: that bypassed the repo editing rule to use `apply_patch` for manual edits, even though the data stayed valid. Corrected rule: use `apply_patch` for hand-authored metadata/doc edits too; reserve scripts for generated files or bulk mechanical rewrites already owned by the repo commands. Location changed: `AGENTS.md`. Impact: future documentation/stat updates stay reviewable as patches.
- Assumption: repo-level npm scripts could be run from the repository root during status/orchestrator setup. Observed result: the root has no `package.json`, so `npm run codex:status` and `npm run orchestrator` failed until the workdir was switched to `web`. Corrected rule: run OpenXcom browser automation from `web` unless invoking an explicit repo-root script path. Location changed: `AGENTS.md` and `openxcom-ts-port` skill. Impact: fewer wasted tool calls on status/build commands.
- Hypothesis: Spark sidecars can save context on runtime-boundary selection if they get exact read-only file lists. Result: two low-effort Spark explorers quickly separated the research cutscene implementation target from the graph persistence verifier gap, while the main agent kept integration local. Rule: use sidecars for bounded source audits and consume only summary-first findings; close the ledger immediately after completion.
- Assumption: research-completion parity could be verified by checking only `ResearchCompleteState` construction. Observed result: the C++ branch queues primary and bonus `CutsceneState` popups before `ResearchCompleteState`, and the report button opens bonus then primary Ufopaedia articles. Corrected rule: for research-completion routes, assert the queued `_popups` order directly after `time1Day()` and monkey-patch `Ufopaedia.openArticle` for report-order proof instead of initializing cutscene media states unnecessarily. Location changed: `GeoscapeState.ts`, `ResearchCompleteState.ts`, `CutsceneState.ts`, and `verify-geoscape-confirmations.mjs`.
- Assumption: JavaScript template literals are safe inside the file-backed verifier string because it is wrapped in `String.raw`. Observed result: Node still interpolates `${...}` before creating the raw string, so the verifier script failed to parse. Corrected rule: inside embedded verifier templates, use string concatenation or escape interpolation markers; reserve template literals for code outside the outer verifier template. Location changed: `web/scripts/verify-geoscape-confirmations.mjs` and `AGENTS.md`.
- Assumption: setting `vector.length = month` was a close enough translation of C++ `vector.resize(month)`. Observed result: JavaScript pads with sparse holes instead of value-initialized zeroes, and the verifier now checks graph-vector padding/rotation explicitly. Corrected rule: when translating C++ vector resize for numeric graph data, push explicit zeroes on growth and verify both first-year resize and post-year rotation. Location changed: `SaveConverter.ts`, `SavedGame.ts`, and `verify-geoscape-confirmations.mjs`.
- Hypothesis: compact seeded DAT buffers can verify original-save conversion without importing full legacy save folders. Result: `verify-saveconverter-metadata.mjs` now seeds `LOC.DAT`, `BASE.DAT`, and `ASTORE.DAT` after the graph/time buffers and proves `loadOriginal()` builds the target table, ordered base list, facilities, stores, and live-alien containment. Rule: original-save reader slices should translate the C++ call order and verify each new DAT reader with the smallest binary fixture that exercises its source offsets.
- Assumption: `LOC.DAT` alien race fields use the same converter list as `ASTORE.DAT`. Observed result: the first verifier failed because `LOC.DAT` uses `converter.crews[dat]`, while `ASTORE.DAT` builds live-alien item ids from `converter.alienRaces[race] + converter.alienRanks[rank]`. Corrected rule: never reuse adjacent converter lists by intuition; assert each DAT reader against the exact source accessor. Location changed: `SaveConverter.ts`, `verify-saveconverter-metadata.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: a CRAFT.DAT verifier should assert the raw speed loaded from the row after destination linkage. Observed result: C++ `Craft::setDestination()` runs after `craft->load()` and resets outbound craft speed to `RuleCraft::getMaxSpeed()`, so the first verifier failed until it asserted the post-source-side-effect speed. Corrected rule: for original-save craft conversion, verify the final object state after the same C++ call order, not only raw DAT field assignments. Location changed: `Craft.ts`, `SaveConverter.ts`, `verify-saveconverter-metadata.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: `SOLDIER.DAT` and `TRANSFER.DAT` can be verified in the same seeded original-save fixture because transfer rows depend on the 250-entry soldier table. Result: the verifier now seeds live and empty soldier slots, then tests item, scientist, engineer, new-craft, soldier, and live-alien transfer branches through one `loadOriginal()` run. Rule: keep original-save dependency order in the fixture as well as the implementation; seed upstream tables before asserting downstream transfer behavior. Location changed: `SaveConverter.ts`, `verify-saveconverter-metadata.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: improved soldier stats from `SOLDIER.DAT` are standalone current stats. Observed result: the C++ reader loads improved stats and then adds the initial stats, while initial bravery uses `110 - 10 * raw` and improved bravery uses `10 * raw`. Corrected rule: original-save soldier conversion must assert both initial and current stats so stat-delta math cannot silently drift. Location changed: `SaveConverter.ts` and `verify-saveconverter-metadata.mjs`.
- Hypothesis: Spark read-only sidecars can save main-thread context on a dense runtime scheduler if they are given exact file scopes. Result: two Spark explorers quickly separated the monthly alien mission source blocks and verifier design while the main agent translated `GeoscapeState::determineAlienMissions()` / `processCommand()` locally. Corrected rule: use sidecars for bounded scheduler audits and fixture plans, but keep hot integration in the main thread; verify monthly mission scripts through real `time1Month()` side effects, nonzero mission delay to avoid unrelated `AlienMission.start()` RNG, and strict strategy logs for mission-run/location/use-table effects. Location changed: `GeoscapeState.ts`, `SavedGame.ts`, `AlienStrategy.ts`, `Mod.ts`, `verify-geoscape-confirmations.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: geoscape save/load could be moved forward as one object-graph slice instead of waiting for full campaign persistence. Result: a source audit showed the C++ ordering constraint (`alienBases -> alienMissions -> UFOs -> waypoints/missionSites -> bases` with craft destination re-linking), and a focused browser round-trip verifier proved the translated `SavedGame.save()/load()` path for waypoints, alien bases, alien missions, UFO mission/trajectory links, mission sites, base craft, and craft waypoint destination restoration. Corrected rule: persistence slices should first translate the shared `Target.save/load/saveId` and `MovingTarget` contracts, then verify through `SavedGame` container side effects instead of isolated object saves. Location changed: `Target.ts`, `MovingTarget.ts`, `AlienBase.ts`, `MissionSite.ts`, `AlienMission.ts`, `Ufo.ts`, `Craft.ts`, `Base.ts`, `SavedGame.ts`, `verify-geoscape-confirmations.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: a persistence sidecar can reduce main-thread context while the main agent wires the hot save graph. Result: a Spark read-only audit extracted the exact C++ `Base::load/save` keys for `ResearchProject` and `Production`, while a verifier worker produced a standalone browser round-trip script; the main agent integrated `SavedGame` production wiring, missing-rule scientist/engineer refunds, and the geoscape verifier locally. Corrected rule: for base child-vector persistence, translate the child class `save/load` shape first, wire it through the owning `Base`/`SavedGame` node, then verify both serialized JSON keys and loaded object fields. Location changed: `SavedGame.ts`, `verify-geoscape-confirmations.mjs`, `verify-research-manufacture-persistence.mjs`, `package.json`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: transfer persistence could be verified as a narrow base-field slice. Result: the read-only sidecar showed `Transfer` also depends on faithful `Soldier.save/load`; the worker verifier first failed at missing transfer serialization, then passed after translating `Transfer.save/load`, `Soldier.save/load`, `Options.soldierDiaries`, and `SavedGame` base `soldiers` / `transfers` / `inBattlescape` / `retaliationTarget` wiring. Corrected rule: when a persisted child object embeds another source object, translate the embedded object's save/load contract before claiming the parent vector is complete. Location changed: `Transfer.ts`, `Soldier.ts`, `SoldierDeath.ts`, `SavedGame.ts`, `Options.ts`, `verify-geoscape-confirmations.mjs`, `verify-base-transfer-persistence.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: the remaining base/debriefing persistence gap could be retired without importing heavy runtime modules into `SavedGame`. Result: `BaseFacility.save()`, `deadSoldiers`, and mission-statistic round-trips were wired while keeping `MissionStatistics` as a type/plain-data shape in `SavedGame`, avoiding a runtime import of its battlescape `TileEngine` dependency. Corrected rule: when a save container only needs to preserve a source-shaped record, store/load the serialized data shape and let UI owners rehydrate richer classes if they need methods. Location changed: `BaseFacility.ts`, `SavedGame.ts`, `verify-geoscape-confirmations.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: multiple verifier scripts could run in parallel because they test disjoint behavior. Observed result: each verifier starts with `npm run build`, and concurrent builds raced while deleting `web/dist`, producing Windows `EPERM` failures on `dist/Interface` and `dist/Mod`. Corrected rule: run build-owning verifier scripts serially, or create a no-build verifier mode before parallelizing them; keep parallelism for read-only status/typecheck/agent work. Location changed: `AGENTS.md` and `openxcom-ts-port` skill. Impact: verification failures now indicate translated behavior instead of local build-output races.
- Hypothesis: the country/region campaign persistence boundary could be retired by translating the small child objects first. Result: `Country.save/load`, `Region.save/load`, and `SavedGame` top-level `countries` / `regions` wiring now preserve source keys, including `funding`, `activityXcom`, `activityAlien`, `pact`, and `newPact`. Corrected rule: for top-level rule-owned save vectors, translate the child object's exact conditional key shape, load by rule id from `Mod`, and verify with explicit fake rule ids instead of assuming the active ruleset has a convenient fixture count. Location changed: `Country.ts`, `Region.ts`, `SavedGame.ts`, `verify-geoscape-confirmations.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: read-only sidecars can map the next persistence frontier while the main thread integrates a smaller hot slice. Result: the SaveConverter sidecar identified `RuleConverter`/`converter.rul` plus core DAT readers as the next coherent original-save slice, while the SavedBattleGame sidecar took too long until interrupted for a partial summary. Corrected rule: for sidecar audits, ask for bounded key lists and partial-return behavior up front; interrupt slow explorers once their result is no longer on the critical path. Impact: main-thread integration kept moving while still capturing next-boundary facts.
- Assumption: waiting for an early loaded rule such as `STR_GROUND` was enough before checking converter metadata. Observed result: `RuleConverter` loads late in `Mod.loadAll()`, so the browser verifier saw an empty converter while other rule tables were already populated. Corrected rule: original-save converter verifiers must wait for the exact needed converter offset/list value, such as `Mod.getConverter().getOffset("BASE.DAT_FACILITIES") === 0x16`, before asserting metadata. Location changed: `Mod.ts`, `RuleConverter.ts`, `SaveConverter.ts`, `verify-saveconverter-metadata.mjs`, `package.json`, `PORTING.md`, and `translation-slices.json`.
- Assumption: a naturally loaded xcom1 Ufopaedia article would contain a zero-cost research requirement suitable for `UP.DAT` conversion proof. Observed result: the verifier found no such article, so the first browser run failed before exercising `loadDatUp()`. Corrected rule: when base game data lacks a natural edge case, mutate the verifier scenario using real loaded rule objects instead of weakening production code; here the fixture attaches a real zero-cost research rule to a real available article, then checks `UP.DAT` unlock behavior. Location changed: `verify-saveconverter-metadata.mjs`.
- Repeated mistake: despite the documented rule, two `agents:start` ledger mutations were launched through one parallel tool call. Observed result: the JSON ledger briefly kept only one active entry and needed a sequential repair. Corrected rule: never wrap ledger-mutating commands in `multi_tool_use.parallel`; use one shell command per ledger mutation and reserve parallel tool calls for read-only commands or independent agent execution. Location changed: `AGENTS.md`.
- Assumption: final `loadOriginal()` state could prove both `ZONAL.DAT` and `ACTS.DAT` strategy conversion. Observed result: faithful C++ `AlienStrategy::load()` clears `_regionChances` and `_regionMissions` on each call, so `loadDatActs()` clears the region chances loaded moments earlier by `loadDatZonal()`. Corrected rule: keep that source behavior, verify `loadDatZonal()` in a tiny isolated reader scenario, and verify final `loadOriginal()` for the lasting ACTS/MISSIONS state and mission-cache handoff. Location changed: `SaveConverter.ts` and `verify-saveconverter-metadata.mjs`.
- Assumption: listing original saves could use JSON metadata separate from the DAT files. Observed result: that did not prove the real C++ `SAVEINFO.DAT` parsing path and diverged from `SaveConverter.binaryBuffer()` storage keys. Corrected rule: browser original-save ingestion should store selected `GAME_#` files under the same localStorage path used by `binaryBuffer()`, and `SaveConverter.getList()` should parse the real binary `SAVEINFO.DAT` header for name/date/time/tactical flags. Location changed: `SaveConverter.ts`, `ListLoadOriginalState.ts`, and `verify-saveconverter-metadata.mjs`.
- Assumption: once `BattleActionType` was split out of `BattlescapeGame`, `SavedGame` could safely import `SavedBattleGame` as a value. Observed result: `SavedBattleGame` still imports `TileEngine`, which imports attack states and can pull `State` before initialization. Corrected rule: campaign save containers must not value-import the full battlescape graph; use a lightweight registry or delayed hydration for `battleGame` payloads, and verify browser startup after every save/model import change. Location changed: `BattleAction.ts`, `SavedBattleGameRegistry.ts`, `SavedGame.ts`, `SavedBattleGame.ts`, and `verify-saveconverter-metadata.mjs`.
- Hypothesis: small read-only Spark explorers can identify whether a stale slice boundary is real before the main thread edits. Result: one explorer confirmed all `SaveConverter` DAT methods, including TFTD `SITE.DAT`, were already translated; the other pointed to saved-battle persistence/resume as the real remaining boundary. Rule: before translating a named remainder, ask sidecars for exact missing source branches and keep the main thread on integration. Impact: avoided reworking already translated `SITE.DAT` and moved directly to `battleGame` save/load/resume parity.
- Assumption: `Tile::serializationKey.totalBytes` was 22 and an `openDoorWest` fixture without map resources could prove binary door-bit persistence. Observed result: the C++ formula is `4 + 2*4 + 2*4 + 1 + 1 + 1 = 23`, and `Tile.saveBinary()` only emits UFO door bits through `isUfoDoorOpen()`, which requires a rebound UFO-door `MapData` object on the wall part. Corrected rule: for binary save parity, derive constants directly from the C++ formula and make verifier fixtures bind the real or fake map object required by the source predicate; do not infer from the text-save flag alone. Location changed: `Tile.ts`, `SavedBattleGame.ts`, `verify-saveconverter-metadata.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: a bounded read-only Spark audit can identify the next post-persistence runtime gap without pulling large battle code into the main context. Result: the explorer found `SavedBattleGame::loadMapResources()` missing the C++ lighting/FOV recalculation sequence, and the main thread implemented and verified that exact path. Rule: after a save/load boundary is serialized, ask a sidecar for the next C++ lifecycle method with a tight file list, then translate one small verified runtime step locally. Location changed: `SavedBattleGame.ts`, `verify-saveconverter-metadata.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: late battlescape runtime gaps move faster when translated as complete small C++ lifecycle methods instead of one isolated symptom at a time. Result: translating `SavedBattleGame::prepareNewTurn()`, `randomizeItemLocations()`, and `resetTiles()` together exposed the real method boundaries and made one compact verifier cover fire/smoke spread, terrain-lighting refresh, storage item movement, and the visibility-reset correction. Rule: for source methods under roughly one screen of C++ with available helpers, translate the full method body and verify several source side effects in one browser scenario before updating progress. Location changed: `SavedBattleGame.ts`, `verify-saveconverter-metadata.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: original-save constructor-level validation could be copied directly into the browser `SaveConverter` shape. Observed result: existing source-shaped helper verifiers construct `SaveConverter` without a selected folder, while browser import/load paths are where actual user-selected `SAVEINFO.DAT` errors surface. Corrected rule: preserve C++ validation intent at the browser boundary that owns the files: reject unrelated imports, missing `SAVEINFO.DAT`, and invalid `SAVEINFO.DAT` in `importOriginalFiles()` / `getList()`, while keeping helper construction usable for isolated DAT reader tests. Location changed: `SaveConverter.ts`, `ListLoadOriginalState.ts`, `verify-saveconverter-metadata.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: tight read-only Spark explorers can identify small runtime parity gaps while the main thread integrates locally. Result: two explorers found exact saved-battle and battle-runtime targets; the main thread immediately translated `SavedBattleGame::setDebugMode`, the ammo-link rule-skip branch in `SavedBattleGame::load`, and the `Tile::setUnit` -> `BattleUnit::setTile(tileBelow)` side effects, then verified all three in the existing saveconverter browser script. Corrected rule: use explorers to shortlist exact method gaps, then batch several disjoint tiny source contracts into one focused verifier when they share the same runtime slice. Location changed: `BattleUnit.ts`, `Tile.ts`, `SavedBattleGame.ts`, `verify-saveconverter-metadata.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: the next Pathfinding explorer target meant the private `bresenhamPath()` / `aStarPath()` guard itself was the best edit. Observed result: source inspection showed those private methods are normally called after `_unit` is set, while the stronger movement-runtime mismatch was that `UnitWalkBState` and `UnitFallBState` still dropped the `tileBelow` argument when updating tile occupancy after Pathfinding-driven movement/falling. Corrected rule: reconcile explorer targets against source accessibility and call-chain evidence; when the direct target is only a guard, inspect the surrounding source call-sites for the real behavioral mismatch. Location changed: `UnitWalkBState.ts`, `UnitFallBState.ts`, `verify-saveconverter-metadata.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: Spark workers can safely handle isolated late battle-runtime translation when write scopes are disjoint. Result: one worker normalized `TileEngine::calculateLine` in `TileEngine.ts`, while another audited `AIModule::getSpottingUnits` and correctly reported no edit was needed; the main thread kept Pathfinding verification local and avoided file collisions. Rule: use Spark for file-owned battle runtime leaves, but keep verifier design and cross-file integration in the main thread. Location changed: `TileEngine.ts`, `verify-battle-runtime.mjs`, `package.json`, and docs/status.
- Assumption: the new battle-runtime verifier could use a simpler Node `spawn("npm.cmd", ...)` helper and assert that A* fallback must be longer than a blocked straight path. Observed result: Windows rejected direct `.cmd` spawning with `EINVAL`, and A* found a diagonal detour with the same number of steps. Corrected rule: copy the existing `.cmd` wrapper pattern for verifier helpers, and assert source outcomes rather than overfitting a path-length guess. Location changed: `verify-battle-runtime.mjs` and `AGENTS.md`.
- Assumption: adding one more module export to a `Promise.all()` verifier import list was mechanically safe. Observed result: `VoxelType` came from the same `MapData` import as `TilePart`, but the destructuring added an extra array slot and failed at runtime. Corrected rule: when a verifier imports multiple symbols from the same module, destructure them from the same `Promise.all` result entry and rerun the browser verifier before updating status. Location changed: `verify-battle-runtime.mjs` and `AGENTS.md`.
- Hypothesis: the next reaction LOS target was more `canTargetUnit` production work. Result: source/TS inspection showed the potential-unit path already matched C++, so the main thread added browser coverage for the compiled potential-unit line-of-fire path instead of inventing code. A mini sidecar then mapped the next true battle-runtime boundary to `BattlescapeGame::checkForCasualties()` and the projectile/explosion aftermath handoff. Rule: when a named boundary is already source-faithful, spend the slice on verification and move the next translation target to the source branch with real missing behavior.
- Assumption: `BattlescapeGame::checkForCasualties()` needed a broad projectile/explosion scenario before it could be verified. Observed result: a compact C++-shaped fake `SavedBattleGame`, `SavedGame`, and `BattleUnit` fixture could call the translated method directly while satisfying the immediate `UnitDieBState` constructor contract. Corrected rule: for parent runtime methods that immediately construct child states, fake only the child constructor's source dependencies and assert parent-side accounting first; save the wider projectile/explosion chain for the next integration slice. Location changed: `BattlescapeGame.ts`, `SoldierDeath.ts`, `verify-battle-runtime.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: a direct `TileEngine.hit()` verifier only needed tile/unit damage methods. Observed result: the production method immediately runs source lighting/FOV refresh, so the fake tile also needed `resetLight()`, `addLight()`, `getShade()`, map-data `getLightSource()`, and save `getGlobalShade()`. Corrected rule: when verifying low-level battlescape runtime methods, include the post-effect lighting contract in the fixture instead of stubbing around it; failing fixtures should reveal missing source dependencies, not encourage weakening production code. Location changed: `verify-battle-runtime.mjs`.
- Hypothesis: a read-only Spark sidecar could audit the projectile aftermath diff while the main thread finished integration. Result: Spark was at capacity, so the sidecar produced no useful work; the main thread continued locally, kept the task unblocked, and closed the ledger entry. Corrected rule: treat model-capacity failures as an immediate fallback signal, not a retry loop; record the failed experiment, then use local scripts/verifiers or a smaller bounded worker only if it materially advances the slice.
- Assumption: adding `BattleType` to the battle-runtime verifier could use another `RuleItem.js` import entry. Observed result: this repeated the exact class of bug already learned from the `VoxelType` import mistake, so it was corrected before the verifier run by destructuring `ItemDamageType` and `BattleType` from the same module entry. Corrected rule: when touching verifier import lists, first consolidate same-module symbols, then add behavior assertions; do not let a harmless duplicate become future import drift. Location changed: `verify-battle-runtime.mjs` and `AGENTS.md`.
- Hypothesis: after a Spark capacity miss, a tiny mini read-only sidecar could still save main-thread context by mapping one exact C++ branch. Result: the sidecar returned the `ProjectileFlyBState.cpp:580-622` shotgun pellet loop and verifier risks while the main thread translated locally; integration stayed on the critical path and the agent was closed promptly. Corrected rule: use mini sidecars for narrow line-map audits when Spark is unavailable, but keep production edits and verifier integration local unless the write scope is disjoint.
- Assumption: shotgun pellet verification would need replacing the imported `Projectile` class with a test double. Observed result: a better browser verifier used the real compiled `Projectile` and a fake `TileEngine.calculateLine()` to make secondary impacts deterministic, proving extra pellet traces, hit animations, `TileEngine.hit()`, and firing-XP capping without import monkey-patching. Corrected rule: for projectile branch verifiers, fake the lower source-owned dependency (`TileEngine`) before trying to replace module imports. Location changed: `ProjectileFlyBState.ts`, `verify-battle-runtime.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: end-of-projectile cleanup could be represented by directly lowering aim and invalidating cache. Observed result: the C++ cleanup path first clears battlescape mouse scrolling, runs reaction fire, and calls `BattleUnit::abortTurn()` on surviving shooters; weapon lowering is handled by the lower-weapon explosion branch. Corrected rule: keep projectile cleanup and explosion lower-weapon cleanup as separate source branches, and verify their order through `think()` rather than only direct private helper calls. Location changed: `ProjectileFlyBState.ts`, `verify-battle-runtime.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: battlescape explosion audio could be patched only inside `ExplosionBState`. Observed result: source sound and animation ids are mutable `Mod` constants (`smallExplosion`, `largeExplosion`, `explosionOffset`), so faithful translation required adding the constants and ruleset override parsing in `Mod.ts` before wiring sounds/frames in `ExplosionBState`. Corrected rule: when source runtime uses `Mod::CONSTANT` for sounds or sprites, first translate the constant default and ruleset override path, then consume it from the battle state. Location changed: `Mod.ts`, `ExplosionBState.ts`, `verify-battle-runtime.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: a minimal arcing-shot verifier only needed `getArcingShot()` and fire-sound fields on the fake weapon rule. Observed result: real `Projectile.calculateThrow()` calls `applyAccuracy()`, which reads aim/min/snap/auto range and dropoff even in a narrow branch test. Corrected rule: when a verifier drives the real projectile path, fake weapon/ammo rules must satisfy the full projectile accuracy contract, not just the branch condition under inspection. Location changed: `verify-battle-runtime.mjs`.
- Mistake: a Bash heredoc was used in PowerShell while updating JSON progress. Observed result: PowerShell treated `<<` as invalid redirection and the command failed before touching files. Corrected rule: in this Windows workspace, pipe a PowerShell here-string to Node (`@' ... '@ | node -`) for short JSON maintenance scripts, or use `apply_patch` for manual text edits.
- User correction: the port is a faithful translation, with browser-only graphics/sound/storage/input boundaries, not a place to invent tactical logic. Observed result: `UnitWalkBState::playMovementSound()` was still empty even though the C++ method was self-contained and only needed existing browser `Sound`/`Mod` adapters. Corrected rule: prioritize empty browser-adapter hooks with direct C++ bodies; translate the source `Mod::CONSTANT` defaults/ruleset overrides first, wire the call-site, and verify through the existing compiled browser runtime script instead of a new one-off harness. Location changed: `Mod.ts`, `UnitWalkBState.ts`, `BattleUnit.ts`, `verify-battle-runtime.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: a Spark read-only explorer can help select the next broad graphics adapter without pulling `Map.cpp` into the main context. Result: the sidecar identified Camera input/timer logic and Map lifecycle/projectile-smoothing/resource setup as the safe first Map/Camera batch, while deferring full sprite rasterization until the browser `SurfaceSet` and `UnitSprite` dependencies are integrated. Rule: use Spark for exact file-scoped graphics audits, then keep production integration local and source-shaped.
- Hypothesis: Map/Camera graphics parity can advance without first porting the full 700-line terrain renderer. Result: translating the source edge-scroll/trigger-scroll/keyboard-scroll timers, wheel level changes, obstacle timeout, and smooth-camera flags gave real browser-adapter parity with small, verifiable state transitions. Corrected rule: for large graphics files, first translate non-raster lifecycle/input/timer contracts that are independent of sprite blitting, then take `drawTerrain()` / `drawUnit()` as a separate renderer batch. Location changed: `Camera.ts`, `Map.ts`, `Options.ts`, `verify-battle-runtime.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: a Spark explorer with a compact prompt would always be cheap enough for renderer sidecar work. Observed result: one read-only Spark sidecar exceeded its model context window before doing useful work, while a second tight source-audit sidecar returned the exact `Map.cpp` layer order. Corrected rule: keep Spark prompts extremely file-scoped and summary-first, close failed sidecars immediately, and fall back to local scripted inspection instead of retrying an oversized prompt. Location changed: `AGENTS.md`. Impact: future agent use should save context rather than spend it.
- Assumption: `UnitSprite.blit(cache)` was equivalent to the C++ `Surface` cache copy. Observed result: browser canvas blits update the canvas, but `Map::drawUnit` parity needs indexed pixels because `blitNShade()` reads the `Surface` pixel buffer. Corrected rule: when a translated C++ path later shades or samples a browser `Surface`, copy indexed pixels (`draw()` + `copy()` or explicit pixel loops), not only the canvas. Location changed: `Map.ts`, `UnitSprite.ts`, and `verify-battle-runtime.mjs`.
- Assumption: typing `BattleUnit.setCache()` as `Surface | null` was enough after adding the real cache array. Observed result: existing translated C++ call sites still use the source null idiom `setCache(0)`, and `tsc --noEmit` caught those calls. Corrected rule: preserve C++ zero/null call contracts on translated public methods unless every call site has already been normalized. Location changed: `BattleUnit.ts`.
- Hypothesis: the first broad `Map.cpp` renderer slice can be translated directly if browser-only work is kept to resource loading and indexed surface plumbing. Result: the slice now loads original battlescape `UFOGRAPH` and `UNITS` PCK/TAB sets, translates `Tile::getSprite`, `BattleUnit::setCache/getCache`, `Map::cacheUnit`, and first `drawTerrain/drawUnit` sprite order, with verifier coverage for terrain sprite blits and unit cache pixels. Rule: for graphics, translate the C++ draw order first and mark only true browser adapter seams: manifest resource discovery, `SurfaceSet` loading, canvas/indexed-pixel copying, and fallback markers when original resources are absent. Location changed: `build.mjs`, `Mod.ts`, `MapData.ts`, `Tile.ts`, `BattleUnit.ts`, `Map.ts`, `UnitSprite.ts`, `verify-battle-runtime.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: remaining `Map.cpp` mask/projectile details might need new browser clipping logic. Observed result: OpenXcom already had the needed `GraphSubset` contract and `Surface::blitNShade(..., GraphSubset)` overload; the faithful port was to wire that existing source shape into the browser indexed `Surface` and call it from translated `Map::drawUnit`, then move projectile drawing back into the terrain tile loop. Corrected rule: before inventing graphics behavior, check whether the C++ helper already exists and translate the helper/overload first; keep browser work to the indexed surface adapter. Location changed: `Surface.ts`, `Map.ts`, `verify-battle-runtime.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: `NumberText` was already usable because it rendered through browser canvas blits. Observed result: `Map.cpp` uses `NumberText` as a source `Surface`, then `blitNShade()` reads indexed pixels for waypoint and path TU overlays; canvas-only drawing left the indexed buffer empty and the verifier caught missing overlay pixels. Corrected rule: any translated UI helper that is later used as a `Surface` source must write its indexed pixel buffer, not only draw to canvas. Location changed: `NumberText.ts`, `Map.ts`, and `verify-battle-runtime.mjs`.
- Hypothesis: the remaining `Map.cpp` display edges could be translated directly from the working C++ body instead of designing new tactical behavior. Result: source-copying the projectile camera branch and UFO Extender accuracy branch also exposed cheap adjacent cursor divergences: non-normal cursor types must force size 1, below-level front cursor frame is 5, and special cursor modes draw a second front-layer frame. Corrected rule: when porting `Map::drawTerrain()`/cursor branches, copy the full adjacent source block and verify state side effects plus text/color values, not only pixels. Location changed: `Map.ts`, `BattlescapeState.ts`, `verify-battle-runtime.mjs`, `PORTING.md`, `translation-slices.json`, and `AGENTS.md`.
- Assumption: browser fallback rendering could keep drawing unit markers when a unit cache was missing. Observed result: `Map.cpp::drawUnit()` returns immediately on a null cached surface, while adjacent source branches also require `BattlescapeMessage`, `Map::setPalette()`, and `animate()` unit upkeep to be copied as actual Map ownership paths. Corrected rule: remove invented renderer fallbacks once the C++ owner path is known; verify hidden-message routing, palette propagation, unit breathing/constant-animation recache, and cache-miss no-op behavior directly. Location changed: `Map.ts`, `verify-battle-runtime.mjs`, `PORTING.md`, `translation-slices.json`, and `AGENTS.md`.
- Assumption: the remaining Menu/Geoscape console markers represented broad missing systems. Observed result: all five were reachable C++ call edges whose child states already existed in TypeScript, so the correct translation was direct `pushState(new ChildState(...))` wiring plus one shared `TargetLike` contract cleanup. Corrected rule: for user-facing route markers, first prove the C++ handler and child constructor, then remove the boundary in bulk and add a cheap route verifier so the same class of stub does not return. Location changed: `NewBattleState.ts`, `InterceptState.ts`, `MissionDetectedState.ts`, `UfoDetectedState.ts`, `SelectDestinationState.ts`, `TargetInfoState.ts`, `Target.ts`, `verify-menu-geoscape-routes.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: translating `NewBattleState::initSave()` could push the sandbox base into a fresh `SavedGame` exactly like C++ without checking the TS constructor. Observed result: TS `SavedGame` starts with a default empty base, so the first browser verifier found two bases and the wrong first-base craft count. Corrected rule: when C++ pushes into an empty container, inspect TS constructors for default placeholder children and clear them before source-shaped load/push logic. Location changed: `NewBattleState.ts`, `SavedGame.ts`, `verify-new-battle-sandbox.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: once `NewBattleState::btnOkClick()` was moved to `BattlescapeGenerator`, the older deterministic browser tactical shortcut could be harmlessly left behind as dead code. Observed result: that kept invented tactical behavior in the translation target and obscured whether the C++ route was truly the only production path. Corrected rule: when a direct source route replaces an earlier browser shortcut, delete the stale fallback helpers in the same slice and verify the source route through the real stack handoff. Location changed: `NewBattleState.ts`, `verify-new-battle-sandbox.mjs`, `PORTING.md`, and `translation-slices.json`.
- Hypothesis: a Spark verifier worker can handle a disjoint browser harness while the main thread translates production code. Result: the worker produced useful branch coverage and reported a passing run, but stayed open longer than needed; interrupting it for a concise status let the main thread review, tighten restoration/branch selection, and run the verifier locally. Corrected rule: for verifier workers, assign one file, keep production integration local, and if the worker is no longer on the critical path, interrupt for a summary before final local verification. Location changed: `verify-new-battle-sandbox.mjs` and `AGENTS.md`.
- Assumption: translating a C++ route into `pushState(new PauseState(OPT_BATTLESCAPE))` could use normal top-level imports from `BattlescapeState`. Observed result: importing `PauseState` pulled `OptionsBattlescapeState -> OptionsBaseState -> BattlescapeState` and the browser failed at startup with `Cannot access 'OptionsBaseState' before initialization`. Corrected rule: when a source route crosses hot UI families, split tiny shared constants into lightweight modules and lazy-load child states from the popup/menu owner so source-shaped routes do not create new value-import cycles. Location changed: `BattlescapeState.ts`, `PauseState.ts`, `OptionsBaseState.ts`, `OptionsOrigin.ts`, and `verify-battle-runtime.mjs`.
- Assumption: a browser verifier can monkey-patch `Game` stack/cursor methods across an awaited lazy import without pausing the real animation loop. Observed result: the frame loop ticked during `finishBattle()` route verification and called the patched stack/cursor methods from unrelated startup state code, producing false failures. Corrected rule: when a verifier temporarily replaces global `Game` methods across `await`, also replace `game.cycle` with a no-op and restore it in `finally`, or use a fully source-shaped fake object whose contract is safe for unrelated ticks. Location changed: `verify-battle-runtime.mjs`.
- Assumption: the worker-proposed `finishBattle()` verifier should assert that the method sets the abort flag because that looked useful for debriefing. Observed result: the C++ `BattlescapeState::finishBattle()` body only uses the `abort` parameter for cutscene selection; callers such as chrono/abort states set `SavedBattleGame::setAborted()` before calling it. Corrected rule: when sidecar guidance conflicts with source, trust the C++ body and record the difference instead of baking invented side effects into production or tests. Location changed: `BattlescapeState.ts`, `verify-battle-runtime.mjs`, and `AGENTS.md`.
- Assumption: setup-objective parity could be represented by copying deployment objective fields when starting a generated battle. Observed result: `BattlescapeGenerator::setupObjectives()` scans generated map tiles for matching floor/object special markers and clamps required objective count to actual marker count. Corrected rule: when a helper's C++ body is small and data-dependent, translate the helper itself instead of approximating its final fields; verify with map-marker fixtures. Location changed: `BattlescapeGenerator.ts` and `verify-battle-runtime.mjs`.
- Assumption: a verifier should assert an intermediate side effect from an early C++ block inside `nextStage()`. Observed result: the unconscious carried-unit position is first set to the old carrier tile, then later moved again when the item is carried into the next stage craft inventory tile. Corrected rule: for long source methods, assert final externally observable state unless the verifier pauses the method at the exact intermediate boundary. Location changed: `verify-battle-runtime.mjs`.
- Assumption: the `/status` Spark bucket could be trusted after it showed capacity remaining. Observed result: an empirical tiny Spark sidecar spawn failed immediately with a usage-limit message, while the local `codex:status` script still cannot report separate model buckets. Corrected rule: treat `/status` model buckets as advisory only; when a sidecar spawn fails, close the agent and local ledger once, record the result, and continue locally or with another model instead of retrying stale capacity data. Location changed: `AGENTS.md`.
- Assumption: `PORTING.md` "not translated yet" wording could remain broad while verifier evidence moved ahead. Observed result: the docs overstated gameplay gaps after the XCOM1 startup/geoscape/new-battle/battlescape/debriefing/save-load path had focused browser coverage. Corrected rule: docs must separate missing translation from browser adapter seams, deeper parity edges, and unverified polish so progress percentages do not undercut the actual source-backed path. Location changed: `PORTING.md` and `AGENTS.md`.
- Assumption: audio volume could remain implicit because `Sound.play()` already read `Options.soundVolume`. Observed result: the C++ source owns more behavior in `Game::setVolume()`: exponential mixer curve, ambient channel 3 scaling by saved-battle ambient volume, and reserved UI channels 1/2 used by button/window sounds. Corrected rule: when a browser adapter reads an option directly but the source has a central mixer method, translate the central method and make adapter playback consume its channel state. Location changed: `Game.ts`, `Sound.ts`, `Music.ts`, `TextButton.ts`, `Window.ts`, and `verify-battle-runtime.mjs`.
- Review catch: initial `Sound.stop()` port halted loop sources but left each `Sound` instance's `_loopSource` set, which would block later ambient `loop()` restarts. Corrected rule: when translating global stop/cleanup around browser handles, clear both the shared registry and the owning object's source-shaped state, then add a verifier assertion for the lifecycle reset. Location changed: `Sound.ts` and `verify-battle-runtime.mjs`.
- Assumption: replacing the `Options.updateMods()` stub could imply full TFTD runtime loading. Observed result: source inspection and a sidecar audit showed the honest first slice is narrower: xcom1/xcom2 master detection, exactly-one-active-master normalization, `ModInfo.canActivate()`, and grouped FileMap ruleset lists. Corrected rule: keep mod-selection slices explicit about the boundary between source-shaped master/ruleset bookkeeping and the later `Game.loadMods()` / `Mod.loadAll()` ruleset-ingestion thread. Location changed: `Options.ts`, `ModInfo.ts`, `FileMap.ts`, `build.mjs`, and `verify-mod-selection.mjs`.
- User correction: `web/` lacked an entry README even though it is the playable browser package. Corrected rule: runnable subpackages need a local README with play URL, build/serve commands, current playable path, verifier commands, and explicit TFD boundary so users do not have to infer workflow from scripts or chat history. Location changed: `web/README.md`.
- User-reported failure: `npm run build` from `web` failed on Windows with `ENOTEMPTY` while removing `web/dist`. Corrected rule: build scripts that recursively delete generated directories on Windows should use `fs.rm` retry options (`maxRetries`/`retryDelay`) so transient file handles do not break normal user builds. Location changed: `web/scripts/build.mjs`.
- User-reported failure: after browser reload, the cursor was still invisible. Observed result: hiding the browser cursor after loading still depended on the translated cursor having a fresh mouse position, which a reload does not guarantee. Corrected rule: in the browser adapter, keep the native canvas cursor visible and do not blit the translated OpenXcom cursor over it unless a later source-specific need proves otherwise. Location changed: `styles.css`, `Game.ts`, `StartState.ts`, and `verify-startup-browser.mjs`.
- Assumption: verifier assertions could use intuitive helper names such as `ItemContainer.getQuantity()` or `RuleTerrain.getType()`. Observed result: the compiled verifier failed because the translated APIs are `ItemContainer.getItem()` and `RuleTerrain.getName()`. Corrected rule: verifier code must use the actual translated source-shaped contract, not invented convenience names; when an assertion fails at the harness layer, fix the harness and record the API. Location changed: `verify-active-mod-runtime.mjs`.
- Assumption: New Battle terrain options could use the loaded terrain list directly. Observed result: C++ combines deployment terrain names with `RuleGlobe::getTerrains()` and resets the terrain selection to the first valid entry; the simplified TS path hid that source behavior. Corrected rule: mission-generator terrain lists should translate `NewBattleState::cbxMissionChange()` and `RuleGlobe::getTerrains()` together, including empty-deployment globe terrain semantics. Location changed: `NewBattleState.ts`, `RuleGlobe.ts`, and `verify-active-mod-runtime.mjs`.
- User-reported failure: startup font rendering looked wrong. Observed result: `Font.loadTerminal()` drew a browser monospace font even though C++ loads the embedded `DosFont.h` BMP. Corrected rule: when C++ embeds a bitmap/resource table, decode that source data in the browser adapter or generate from it; do not replace it with a platform font. Location changed: `Font.ts` and `verify-startup-browser.mjs`.
- User-reported failure: normal font rendering still looked wrong after the terminal bitmap fix. Observed result: TS defaulted geoscape/battlescape scale to `SCALE_2X` instead of the C++ `SCALE_ORIGINAL`, CSS stretched the 640x400 canvas to a fractional 1.8x at 1280x720, and PNG font shades were guessed from brightness instead of preserving exact indexed grayscale levels. Corrected rule: audit the whole display path for font issues; restore source scale defaults, use integer CSS canvas multiples, and preserve indexed font shade values. Location changed: `Options.ts`, `Screen.ts`, `Surface.ts`, `styles.css`, `verify-startup-browser.mjs`, `PORTING.md`, and `translation-slices.json`.
- User-reported failure: normal font glyphs still had light pixels around them after the first indexed-shade fix. Observed result: C++ lodepng loads `FontSmall.png` as raw indexed PNG data, whose palette index order is inverse of brightness (`255` is source index `1`, `63` is source index `5`); my prior exact mapping reversed it. The new verifier also caught that reference renderers must clip negative-spacing glyph overhangs instead of wrapping writes into the next row. Corrected rule: prove font fixes with `npm run verify:font-source-parity`, source-reference and TS-rendered snapshots, and raw index comparison before relying on visual judgment. Location changed: `Surface.ts`, `verify-font-source-parity.mjs`, `package.json`, `PORTING.md`, and this file. Impact: normal UI font shade order now matches the C++ source path pixel-for-pixel.
- User-reported failure: the planet/globe was not rendered correctly. Observed result: TS `Globe.ts` was an invented renderer with radial ocean shading, meridian grid lines, averaged flat land colors, float zoom, no `TEXTURE.DAT` `SurfaceSet`, no cached polygon projection, no C++ shadow pass, and no `globe.rul` polylines/colors. Corrected rule: for visual parity bugs, do not trust memory or visual guesses; generate a compact source-reference snapshot from original data plus the C++ algorithm, then compare it to the built browser renderer pixel-for-pixel. If a native C++ executable/toolchain is unavailable, state that and use the C++ source/data path as the reference until a native capture path exists. Location changed: `Surface.ts`, `Polygon.ts`, `RuleGlobe.ts`, `Mod.ts`, `Globe.ts`, `verify-globe-source-parity.mjs`, `package.json`, `PORTING.md`, and this file. Impact: core globe rendering now matches the source-reference path with `diffPixels: 0`; remaining globe work is target/radar/flight/label overlays and input/timer polish, not planet texture/shadow rendering.
- User-reported failure: no music played. Observed result: `State.init()` was missing the C++ `RuleInterface::getMusic()` -> `Mod::playMusic()` call, TFTD music can live in GM.CAT rather than direct MID files, and Chromium does not play MIDI natively. Corrected rule: first restore the source music request lifecycle, then adapt the browser backend at the asset/playback boundary with GM.CAT MIDI extraction, user-gesture unlock, and WebAudio MIDI playback. Location changed: `State.ts`, `Mod.ts`, `Music.ts`, and `verify-startup-browser.mjs`.
- User-reported failure: music sounded horrible, followed by correction that OpenXcom streams OGG/digital tracks. Observed result: the browser loader only tried direct `.MID` and GM.CAT, `Options.preferredMusic` used an invented string default instead of the source enum `MUSIC_AUTO`, `CatFile.load()` subtracted the embedded-name bytes even though the C++ skips the name and still reads the object size, and GM.CAT track initialization had two extra bytes. Corrected rule: translate `Mod::loadMusic()` priority first (`preferred, FLAC, OGG, MP3, MOD, WAV, ADLIB, GM, MIDI`) and stream browser-playable digital audio natively; treat MIDI synth work only as fallback. Verify with `npm run verify:music-source-priority`, which proves OGG priority and GM.CAT init bytes. Location changed: `Options.ts`, `Mod.ts`, `Music.ts`, `CatFile.ts`, `GMCat.ts`, `verify-music-source-priority.mjs`, `package.json`, and `PORTING.md`.
- User correction: MIDI JavaScript libraries exist, but OpenXcom should stream OGG/digital audio when present. Observed result: the local original `XCOM\SOUND` and `TFD\SOUND` folders contain MID/GM.CAT/ADLIB.CAT assets but no OGG/MP3/FLAC streams, and the browser's built-in MIDI path was a crude oscillator synth that made valid music requests sound broken. Corrected rule: keep native `Audio` streaming as the first-class path for digital files; for MID/GM.CAT, expose an explicit MIDI backend hook for a real SoundFont/DLS library and disable the crude oscillator by default. Verify both the stream-priority fixture and the no-implicit-oscillator/backend lifecycle with `npm run verify:music-source-priority`. Location changed: `Music.ts`, `AdlibMusic.ts`, `Mod.ts`, `verify-music-source-priority.mjs`, `verify-startup-browser.mjs`, `PORTING.md`, and `translation-slices.json`.
- Audio follow-up: source priority says preferred format is tried first, but in the browser a user-selected GM/MIDI preference can steal playback from an available OGG stream and produce silence or bad synthesized music. Corrected rule: keep source order for real digital formats, but make browser-playable digital streams win before non-digital GM/MIDI/Adlib fallback, and skip unsupported digital formats instead of accepting buffered bytes that later fail as native audio. Verify explicit `MUSIC_GM` and `MUSIC_MIDI` preferences still choose OGG when a stream exists. Location changed: `Mod.ts`, `verify-music-source-priority.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: FLC cutscenes could stay as a browser boundary until later because rules and battlescape paths mattered more. Observed result: `VideoState` was only logging the requested video while the C++ `FlcPlayer` decoder was already available to translate, and the browser manifest did not expose `UFOINTRO` with source-relative FileMap keys. Corrected rule: when a C++ adapter body is self-contained, translate the decoder directly and verify it against a real original asset; also verify the manifest path shape, not only the generated JSON. Location changed: `FlcPlayer.ts`, `VideoState.ts`, `FileMap.ts`, `Options.ts`, `build.mjs`, and `verify-video-flc-player.mjs`.
- Music backend fix: Spark status reported credits, but Spark spawns failed with the account limit; a mini read-only sidecar still identified the remaining MIDI/backend gap. Observed result: XCOM/TFTD local data has no digital streams, so the playable browser path needed a real software MIDI adapter, not more priority tweaking. Corrected rule: treat Spark availability as empirical, close failed agents/ledger entries immediately, and for music use native streaming first plus bundled libtimidity/FreePats for MID/GM.CAT fallback. Verify the real backend in-browser, not just a mock backend. Location changed: `TimidityMidiBackend.ts`, `Music.ts`, `main.ts`, `web/vendor/timidity`, `verify-music-source-priority.mjs`, `verify-startup-browser.mjs`, `README.md`, `PORTING.md`, and `translation-slices.json`.
- Sidecar correction: a read-only audit inferred selected-base persistence from `SavedGame::getSelectedBase()` / `setSelectedBase()` accessors and the current TS save key. Source `SavedGame::save()` / `load()` do not serialize `_selectedBase`. Corrected rule: persistence parity must be audited from the source save/load bodies, not from accessors or existing TS behavior; the faithful fix can be deleting an invented key and proving the omission. Location changed: `SavedGame.ts`, `verify-geoscape-confirmations.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: the `PORTING.md` interface-palette marker meant an undefined animation system, and a direct verifier could swap `State._game` to a fake without affecting the live page. Observed result: the real source gap was the concrete `State::applyBattlescapeTheme()`/`setInterface(..., battleGame)` menu palette branch, and the first verifier failed because the browser frame loop ticked while `State._game` pointed at a partial fake. Corrected rule: map broad docs markers to exact reachable C++ methods, translate that method body first, and when a verifier replaces `State.setGamePtr()` or similar globals, pause `game.cycle` and restore both the game pointer and cycle in `finally`. Location changed: `State.ts`, menu save/options/pause states, `TextList.ts`, `ComboBox.ts`, `verify-interface-battlescape-theme.mjs`, `PORTING.md`, and `translation-slices.json`.
- Assumption: the original-save slice could be marked complete from import/list/DAT-reader coverage alone. Observed result: a small read-only sidecar and local source audit both found the remaining C++ constructor guard: `SaveConverter::SaveConverter()` rejects a missing `GAME_#/SAVEINFO.DAT`. Corrected rule: source constructor guards are translation behavior too; update isolated verifiers to seed required source files, then assert the direct invalid-constructor path instead of leaving validation only at a browser adapter boundary. Location changed: `SaveConverter.ts`, `verify-saveconverter-metadata.mjs`, `PORTING.md`, and `translation-slices.json`.
- User correction: calling this "game logic" is wrong for this project. Observed result: the C++ `SoldierDiary::manageCommendations()` body was already mostly translated; the actual missing behavior was the source loader path, because `Mod.ts` did not ingest top-level `commendations:` rulesets or expose `Mod::getCommendation()` / `getCommendationsList()`. Corrected rule: when a source-shaped class exists but runtime behavior is absent, inspect the owning C++ loader/registry before rewriting the algorithm. Location changed: `RuleCommendations.ts`, `Mod.ts`, `verify-soldier-diary-commendations.mjs`, `PORTING.md`, `translation-slices.json`, and `web/NEXT_AGENT_HANDOFF.md`. Impact: soldier diary commendation rules moved from a documented pending boundary to verified source-backed runtime behavior.
- Assumption: Spark could be used because `/status` showed capacity. Observed result: a tiny no-fork Spark sidecar failed immediately with a usage-limit message, while local status could not report model-specific credits reliably. Corrected rule: model quota is empirical; one failed spawn is enough evidence for the current turn, so close/ignore the failed worker, record the result, and continue locally or with another model instead of retrying. Location changed: `AGENTS.md` and `web/NEXT_AGENT_HANDOFF.md`.
- Handoff discipline: when the user asks to stop before the token limit, preserve project state in repo files, not chat memory. Corrected rule: write a standalone handoff file with current percentages, verified commands, latest changed files, known tool/model limitations, and next continuation steps; then regenerate status/context and commit. Location changed: `web/NEXT_AGENT_HANDOFF.md`, `AGENTS.md`, generated status/context files.

## Reporting Progress

When finishing a slice, include:

- what source files were translated
- what TypeScript files changed
- verification commands/results
- explicit pending source boundaries
- translation percentage for the slice, the area, and the whole browser port

Do not mark the overall goal complete until the full browser TypeScript game translation is actually complete and verified against the original scope.
